<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.triptape.user.repo.UserRepo">
	<insert id="regist">
		insert into user(id, pw, name, nickname, email, tel, profile_img, birthday, gender, isAdmin, salt) 
		values(#{user.id}, #{user.pw}, #{user.name}, #{user.nickname}, #{user.email}, 
			#{user.tel}, #{user.fileInfos.originalFile}, #{user.birthday}, #{user.gender}
			, #{user.isAdmin}, #{salt})
	</insert>

</mapper>