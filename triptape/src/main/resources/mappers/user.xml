<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.triptape.user.repo.UserRepo">
	<resultMap type="com.ssafy.triptape.user.UserDto" id="user">
		<id property="userKey" column="user_key" />
		<result property="isAdmin" column="is_admin" />
		<collection property="profileImg" javaType="com.ssafy.triptape.file.FileInfoDto">
			<result column="saveFile" property="profileImg" />
		</collection>
	</resultMap>

	<insert id="regist">
		insert into user(id, pw, name, nickname, email, tel, profile_img, birthday, gender, is_admin, salt) 
		values(#{user.id}, #{user.pw}, #{user.name}, #{user.nickname}, #{user.email}, 
			#{user.tel}, #{user.profileImg.saveFile}, #{user.birthday}, #{user.gender}
			, #{user.isAdmin}, #{salt})
	</insert>
	
	<select id="login" resultMap="user">
		select * from user where id = #{id}
	</select>
	
	<select id="getsalt">
		select salt from user where id = #{id}
	</select>

</mapper>